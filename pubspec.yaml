name: filesteward
description: A macOS-first FileSteward prototype.
publish_to: 'none'
version: 0.5.4+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_selector: ^1.1.0 # Lets the user choose a folder on macOS.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
